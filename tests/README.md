# Tests and Validation Scripts

This folder contains validation and test-style scripts used during model development.

Some files are executable evaluation scripts rather than strict unit tests. Run them directly:

```bash
python tests/test_evrp.py --help
python tests/test_model_variants.py --help
```
